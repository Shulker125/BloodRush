import java.util.Random;

public class PerlinNoise {
	//Create 2d double array based on the size of map
	//Create algorith to calculate values from -1 to 1 starting at 0
	//each square calculation will be based on the squares around it, taking average of the 4 touching and using that value
	//with that maybe increment by a set range of random values that will be randomly positive or negative
	//This will create different numbers around the map, but will related to one another
	//These values then could be used for biome generation
    
}